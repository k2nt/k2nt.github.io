apiVersion: v1
kind: Service
metadata:
  name: {{ include "carbonedge-tier2.fullname" . }}
  labels:
    {{- include "carbonedge-tier2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      nodePort: {{ .Values.service.nodePort }}
      protocol: TCP
      name: http
  selector:
    {{- include "carbonedge-tier2.selectorLabels" . | nindent 4 }}

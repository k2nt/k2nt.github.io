apiVersion: v1
kind: Pod
metadata:
  name: '{{ include "sinfonia-tier1.fullname" . }}-test-connection'
  labels:
    {{- include "sinfonia-tier1.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sinfonia-tier1.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
